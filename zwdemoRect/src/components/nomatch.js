import React, { Component } from 'react';

const NoMatch = React.createClass({
  
  render() {
    return (
      <div>
        <h2>URL NOT FOUND!</h2>
        {/* etc. */}
      </div>
    )
  }
});
export default NoMatch;